<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
</head>

<body>
  <div class="metric" id="metric3">
    <script>
      var xValue = JSON.parse(' <%- JSON.stringify(Object.keys(pontosMes)) %>');
      var yValue = JSON.parse('<%- JSON.stringify(Object.values(pontosMes)) %>');
      var trace1 = {
        x: xValue,
        y: yValue,
        type: 'bar',
        marker: {
          color: 'rgb(49,130,189)',
          opacity: 0.7,
        }
      };

      var data = [trace1];

      var layout = {
        title: 'Pontos por Mês no ano atual',
        xaxis: {
          tickangle: -45
        },
        barmode: 'group',
      };

      var config = { responsive: true };

      Plotly.newPlot('metric3', data, layout,config);
    </script>
  </div>
</body>

</html>